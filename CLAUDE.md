# 戈尔基康复训练系统 - Claude工作记录

## 项目概述

**项目名称**: 康助侠平板端UI演示系统  
**技术栈**: Vue 3.5 + Vite 5.0 + D3.js + ECharts  
**应用类型**: 医疗级近红外脑氧监测与康复训练平台  
**运行端口**: http://localhost:3000  

### 核心功能
- 实时fNIRS脑氧监测
- 康助侠运动设备集成
- 专业大脑热力图可视化
- 多模式训练界面（专业大脑、传统热力图、数据曲线、交互游戏）
- 训练结果评估与报告生成

## 项目结构

```
src/
├── App.vue                    # 主应用状态管理
├── components/               # 核心UI组件
│   ├── ObelabTrainingView.vue    # 训练界面（主界面）
│   ├── AssessmentView.vue        # 评估界面
│   ├── StandbyView.vue          # 待机界面
│   └── [其他辅助组件]
├── utils/                    # 工具模块
│   ├── HeatmapRenderer.js       # 模块化热力图渲染器
│   ├── HeatmapCore.js          # 数据处理核心
│   └── [配置和几何工具]
└── assets/                   # 静态资源
    └── brain_no_bg.png         # 大脑背景图片(1000x1000)

fnirs_sdk/                    # Python SDK
├── data_structures/          # 数据结构定义
├── processing/              # 信号处理算法
└── config/                  # 设备配置管理
```

## 最近工作进展 (2025-08-30)

### 🚀 新功能：待机界面三步骤预训练流程 (2025-08-30)

#### 需求背景
用户希望在待机界面添加三步骤的预训练流程，确保在开始训练前完成必要的信息登记和设备检查。采用渐进式引导，提升用户体验和数据完整性。

#### 功能设计

##### 1. 三步骤流程
- **① 基础信息**：患者信息登记（基于脑卒中风险评估量表）
- **② 设备校验**：fNIRS设备连接和信号质量检查
- **③ 开始训练**：所有准备工作完成后开始训练

##### 2. 交互设计要点
- **强制顺序执行**：必须按①→②→③的顺序完成
- **状态可视化**：未完成步骤显示为灰色禁用状态
- **模态弹窗**：基础信息采用弹窗形式，背景半透明遮罩
- **本地存储**：患者信息保存到localStorage持久化

##### 3. 基础信息弹窗（分步卡片式）

**步骤1 - 基本信息**
- 姓名输入
- 年龄（数字输入 + 滑块双模式）
- 联系方式

**步骤2 - 身体指标**
- 身高（点击编辑 + 滑块）
- 体重（点击编辑 + 滑块）
- BMI自动计算和可视化显示
- 血压记录

**步骤3 - 健康状况**
- 高血压
- 糖尿病  
- 吸烟史
- 其他风险因素
- "均无以上状况"快捷选项

##### 4. 设备校验（第一阶段简化版）
- 点击后显示检测动画
- 模拟三项检查：帽子佩戴、信号质量、系统自检
- 2秒后自动完成（后续可扩展为真实检测）

##### 5. UI设计规范
- **风格**：沿用现有蓝色医疗风格
- **图标**：使用SVG图标，不使用emoji
- **动画**：淡入淡出 + 轻微缩放效果
- **响应式**：支持平板和桌面端显示

##### 6. 数据结构
```javascript
{
  patientInfo: {
    // 基本信息
    name: String,
    age: Number,
    phone: String,
    // 身体指标
    height: Number,
    weight: Number,
    bloodPressure: {
      systolic: Number,
      diastolic: Number
    },
    // 健康状况
    conditions: {
      hypertension: Boolean,
      diabetes: Boolean,
      smoking: Boolean,
      // ...其他
    },
    // 计算字段
    bmi: Number,
    riskLevel: String
  }
}
```

#### 实施计划

##### 第一阶段（当前实施）
- [x] 需求确认和文档记录
- [ ] StandbyView.vue添加三步骤按钮布局
- [ ] PatientInfoModal.vue组件开发
- [ ] 数字输入和滑块双模式交互
- [ ] BMI实时计算和可视化
- [ ] 设备检查模拟动画
- [ ] localStorage本地存储

##### 第二阶段（未来扩展）
- [ ] 真实设备信号质量检测
- [ ] 风险评分算法实现
- [ ] 根据风险等级推荐训练方案
- [ ] 生成风险评估报告

##### 第三阶段（远期规划）
- [ ] 历史数据对比分析
- [ ] 多用户管理
- [ ] 医疗报告导出
- [ ] 云端数据同步

---

## 最近工作进展 (2025-08-29)

### 🎯 重要里程碑：4项关键系统问题完美修复 (2025-08-29)

**背景**: 用户切换到Opus模型后发现系统存在4个关键问题，经过系统性修复和Playwright MCP自动化验证，全部问题已完美解决。

#### 修复成果总览
- ✅ **数据曲线跳动问题**: 使用SDK统计数据替代432通道重算，曲线平滑稳定
- ✅ **按钮响应式问题**: CSS clamp()实现多分辨率完美适配
- ✅ **评估热力图不显示**: 径向渐变算法优化，热力图清晰可见  
- ✅ **测试API按钮清理**: 界面简洁化，直接使用真实数据流

#### Playwright自动化验证结果
- **视觉验证**: 5张截图确认修复效果 (curve-mode-validation.png等)
- **多分辨率测试**: 1920x1080、1200x800、768x600全部通过
- **功能完整性**: 训练→曲线→评估完整流程验证成功
- **代码质量**: Console日志显示"SDK数据优化"等关键信息确认

> 详细修复报告: [validation-fixes-2025-08-29.md](docs/validation-fixes-2025-08-29.md)

### 🚀 历史任务：专业大脑模式热力图全面重构 (2025-08-28)

#### 任务背景
用户要求重新实现专业大脑模式下的热力图可视化，采用全新的技术架构：
1. **技术栈升级**: 从Canvas改为SVG + D3.js实现
2. **算法优化**: 基于IDW插值的自然扩散边界，去除强制形状约束  
3. **坐标系统**: 直接使用Triangle 2D坐标系，与brain_no_bg.png绑定式缩放
4. **实时性能**: 支持432通道×2Hz的实时数据更新

#### 新开发要求 ⚠️ 重要
**Playwright MCP自动化验证**: 每个开发环节完成后，必须使用Playwright MCP进行自动化验证
- 视觉验证：截图对比确保热力图正确显示
- 功能验证：模拟用户交互验证响应正确性
- 数据验证：检查DOM结构确保数据正确传递
- 性能验证：监控渲染时间确保满足实时要求
- 兼容性验证：多分辨率测试确保自适应效果

#### 技术方案确定
- **架构选择**: SVG + d3.js + d3-contour（自然扩散，无clipPath约束）
- **数据流**: fnirs_sdk → Triangle配置解析 → IDW插值 → d3.contours等高线 → SVG渲染
- **自适应机制**: 参考heatmap_adaptive_demo.html的position/scale逻辑
- **开发文档**: 已创建详细的6阶段开发计划（docs/brain-heatmap-development-plan.md）

### 🎯 历史突破：Triangle热力图渲染架构（已备份）

#### 问题背景
用户反映"热力图并没有画在大脑模式表面呀！没看见呢？"，经过深入分析发现问题的根源：
1. **数据结构错误**: 使用了错误的3D坐标和不正确的通道计算方式  
2. **插值算法错误**: 没有按照光源-检测器对的正确逻辑进行计算
3. **坐标系统错误**: Triangle 2D坐标系到Canvas像素的映射有误

#### 解决方案突破

**1. 数据解析重构（参考correct_864_channel_heatmap.py）**
- 文件：`src/components/training/modes/BrainModeView.vue:92-174`
- 成就：正确识别36个光源（optode_a/b/c）+ 48个检测器（optode_1/2/3/4）
- 算法：通道位置 = (光源位置 + 检测器位置) / 2，完全复制Python逻辑
- 优化：距离过滤30-80mm，从1728理论通道筛选到993有效通道

**2. 坐标系统正确实现**
- 坐标系：Triangle 2D坐标系（188.72 × 110.29 mm）
- 映射算法：使用demo.html的自适应参数 position(0.5, 0.25), scale(0.75, 0.6)
- 插值方式：IDW（反距离权重），基于真实通道位置

**3. 模拟数据优化**
- 算法：基于实际空间位置的激活模式生成
- 逻辑：y_normalized = (position[1] - 15) / (100 - 15)，模拟真实大脑激活
- 效果：顶部区域激活（正值），底部区域抑制（负值）

#### 技术验证结果
- **数据解析**: 36光源 + 48检测器 = 993有效通道 ✅
- **坐标系统**: Triangle 2D坐标系（mm） → Canvas像素坐标 ✅  
- **距离过滤**: 30-80mm合理距离过滤 ✅
- **算法正确性**: 完全复制correct_864_channel_heatmap.py逻辑 ✅

### 🎯 历史问题解决：大脑图片比例修复

#### 问题背景
在1920x1080分辨率下发现3个关键的图片显示问题：
1. 专业大脑模式切换后不显示brain_no_bg.png
2. 专业大脑模式图片存在变形
3. 训练完成界面大脑热力图被拉伸成2:1而非原始1:1比例

#### 解决方案

**1. 模式切换问题修复**
- 在ObelabTrainingView.vue中添加`watch(displayMode)`监听器
- 自动重新初始化专业大脑热力图
- 确保模式切换时图片正常加载

**2. 专业大脑模式Canvas渲染优化**
- 文件：`src/components/ObelabTrainingView.vue:371-396`
- 修复：Canvas使用固定像素尺寸而非百分比
- 实现：700px x 700px + 居中定位 + 响应式限制
- 结果：完美1:1比例显示

**3. 评估界面热力图容器修复**
- 文件：`src/components/AssessmentView.vue:1066-1081`
- CSS修复：`aspect-ratio: 1` + `width: auto` + 固定max尺寸
- Canvas修复：600px x 600px + 居中定位
- 结果：从扭曲的2:1恢复到正确的1:1比例

#### 技术验证结果
- **训练界面**：Canvas 700x700 → 显示 700x700 (ratio: 1.000) ✅
- **评估界面**：Canvas 600x600 → 显示 250x250 (ratio: 1.000) ✅
- **响应式测试**：1920x1080分辨率下完美显示 ✅

### 🔧 技术改进

**核心修复策略**
1. Canvas固定像素尺寸：避免容器拉伸变形
2. CSS aspect-ratio保护：强制维持1:1比例
3. 居中定位算法：`transform: translate(-50%, -50%)`
4. 响应式限制：防止溢出的同时保持比例
5. 智能监听机制：确保模式切换的稳定性

**代码质量提升**
- 添加详细的Canvas配置注释
- 实现模块化的图片比例保护机制
- 优化用户体验：宁愿小一点也不破坏原始比例

### 🔍 关键开发原则 (2025-08-28 重要更新)

#### 【原则】前端可视化开发必须截图验证

**背景**: 在修复热力图坐标对齐问题时，用户指出："会后面你就搞完了截图，不然你不知道问题在哪里"

**核心原则**:
- ✅ **每次修改后立即截图验证**: 代码修改完成后必须使用Playwright MCP截图
- ✅ **视觉验证优于代码审查**: 坐标、布局、样式问题只能通过截图发现
- ✅ **console日志不足以判断**: 数值正确不代表视觉效果正确
- ✅ **边框、对齐、颜色等问题**: 必须通过实际渲染效果验证

**应用场景**:
- 🎯 热力图坐标对齐调试
- 🎯 CSS布局和定位修复  
- 🎯 Canvas/SVG渲染效果验证
- 🎯 响应式设计测试
- 🎯 颜色和视觉样式调整

**工作流程**:
1. 代码修改 → 2. 立即截图 → 3. 分析视觉效果 → 4. 继续调整或确认完成

## 开发环境

### 启动命令
```bash
npm run dev        # 启动开发服务器 (端口3000)
npm run build      # 构建生产版本
npm run preview    # 预览构建结果
npm test           # Playwright测试
```

### 当前运行状态
- ✅ 开发服务器运行中：http://localhost:3000
- ✅ 热重载功能正常
- ✅ Triangle热力图数据架构完整实现
- ✅ 36光源+48检测器+993通道数据解析成功
- 🔄 热力图可视化渲染持续优化中

## 项目特色

### 医疗级UI设计
- Obelab专业风格界面
- 实时数据可视化 (2Hz更新频率)
- 高精度热力图算法 (连续插值)
- 多设备兼容 (6节点、12节点、三角形布局)

### 技术亮点
- 双Canvas分层渲染架构
- 模块化热力图渲染器
- 智能设备集成 (fNIRS + 康助侠)
- 自动化测试覆盖 (Playwright)

## 🎓 开发经验与教训 (2025-08-28 重要更新)

### 【教训1】组件重构时的样式保持原则

#### 问题背景
在执行TrainingView.vue组件拆分任务时，犯了一个严重错误：**在拆分组件时意外改变了原始的界面样式**。

#### 用户反馈（原文）
- "你怎么把我原来界面样式都改掉了？不是不透明的+蓝色吗？"
- "我只是让你拆分，没让你改样式呀！"  
- "还有很多细节，也需要还原！透明的按钮、颜色条出现的位置、logo名称、大脑图片显示的方式等等"

#### 具体错误分析
1. **Header结构变更**：将原始的Golgi标题header改成了顶部colorbar
2. **透明度破坏**：将透明玻璃效果(`rgba(255, 255, 255, 0.1)`)改成几乎不透明(`rgba(255, 255, 255, 0.95)`)
3. **文本颜色变更**：将白色文本改成深色文本
4. **设计风格偏离**：偏离了原始的Obelab专业医疗风格

#### 正确原则（用户偏好）
- ✅ **拆分 ≠ 重新设计**：重构只改代码结构，不改UI外观
- ✅ **保持原始样式**：完全保持透明玻璃效果、蓝色渐变背景、白色文本
- ✅ **保持布局结构**：Golgi header、侧边栏布局、颜色条位置等完全不变
- ✅ **保持交互逻辑**：所有用户交互体验保持一致

#### 修复结果
经过全面修复后，成功恢复了所有原始样式：
- ✅ TrainingContainer.vue：恢复Golgi header和蓝色渐变背景
- ✅ BrainModeView.vue：恢复专业大脑模式的颜色条位置
- ✅ HeatmapModeView.vue：恢复透明热力图卡片样式
- ✅ CurveModeView.vue：恢复简洁透明设计
- ✅ GameModeView.vue：恢复简洁占位符设计

### 【核心开发原则】

#### 重构任务执行标准
1. **明确需求边界**：仔细区分"功能重构"vs"样式重新设计"
2. **样式零变更原则**：除非明确要求，否则UI外观应该完全不变
3. **用户体验一致性**：重构后用户应该感受不到任何界面差异
4. **渐进式修改**：一次只做一类变更，避免多维度同时修改

#### 沟通确认机制
- 📋 任务开始前：明确需求范围和不变要素
- 🔍 过程中验证：关键节点确认是否偏离原始设计
- ✅ 完成后检查：对比原始界面确保视觉一致性

### 【应用示例】
```
❌ 错误理解："重构组件" = 可以优化UI设计
✅ 正确理解："重构组件" = 仅重新组织代码结构

❌ 错误做法：拆分时顺便"改进"样式和布局
✅ 正确做法：完全复制原始样式到新组件中
```

### 【教训2】热力图算法实现的深度技术分析原则

#### 问题背景
在实现Triangle热力图可视化时，遇到"热力图不显示"的复杂问题，经过深入分析发现这是一个**多层次的技术架构问题**。

#### 用户关键指导（原文）
- "先把思路整理好，不着急直接开干，方案准备好"
- "坐标映射完了就是要根据图片的大小自适应调整"  
- "应该2D坐标就就够了"
- "要根据 光源和检测器中心位置 对应的通道去画"

#### 技术分析方法论

**1. 分层问题诊断法**
- ✅ **数据层**: 先确认SDK数据结构（36光源+48检测器）
- ✅ **算法层**: 参考标准实现（correct_864_channel_heatmap.py）  
- ✅ **坐标层**: 理解坐标系统（Triangle 2D → Canvas像素）
- ✅ **渲染层**: 自适应映射（demo.html位置参数）

**2. 标准参考实现法**
- 找到Python参考实现：correct_864_channel_heatmap.py
- 完全复制核心算法：`通道位置 = (光源位置 + 检测器位置) / 2`
- 保持数据格式一致：Triangle 2D坐标系（mm）
- 应用相同的距离过滤：30-80mm合理距离

**3. 渐进式验证法**
- 步骤1: 数据解析验证 → ✅ "36光源, 48检测器, 993通道"
- 步骤2: 坐标系统验证 → ✅ Triangle 2D坐标系正确
- 步骤3: 算法逻辑验证 → ✅ 复制Python逻辑成功
- 步骤4: 可视化调试验证 → 🔄 持续优化中

#### 关键技术领悟

**坐标系统是核心**：用户强调"坐标映射完了就是要根据图片的大小自适应调整"，这揭示了fNIRS可视化的核心挑战：
- Triangle物理坐标系（mm） 
- Canvas像素坐标系（px）
- 大脑图片坐标系（相对位置）
- 三层坐标系的正确转换是成功的关键

**参考实现的重要性**：有标准Python实现时，不要重新发明轮子，直接移植核心算法。

**用户领域expertise**：用户对fNIRS系统有深度理解，其指导往往直击核心技术要点。

## 下一步计划

### 待优化项目
- [ ] Triangle布局数据加载错误修复
- [ ] ECharts图例组件导入警告解决
- [ ] 数据持久化功能完善
- [ ] 设备连接稳定性优化

### 功能扩展
- [ ] 多语言支持
- [ ] 数据导出功能增强
- [ ] 历史记录管理
- [ ] 用户权限系统

---

**最后更新**: 2025-08-28  
**更新人**: Claude (AI Assistant)  
**当前版本状态**: 稳定运行，核心功能完整