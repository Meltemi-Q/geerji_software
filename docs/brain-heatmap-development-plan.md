# 专业大脑模式热力图开发计划

**项目目标**: 在专业大脑模式下实现基于D3.js的IDW插值热力图可视化  
**技术方案**: SVG + D3.js + 自然扩散边界  
**开发日期**: 2025-08-28  

## 📋 技术需求确认

- [x] **需求分析**: 查看参考文件了解热力图需求和实现方式
- [x] **数据结构**: 分析fnirs_sdk的triangle配置和数据结构  
- [x] **方案优化**: 去除clipPath约束，使用自然扩散
- [x] **依赖检查**: 确认d3@7.9.0和d3-contour@4.0.2已安装

## 🏗️ 阶段1：基础架构搭建

### 1.1 创建核心模块文件
- [ ] 创建 `src/components/training/modes/heatmap/` 目录
- [ ] 创建 `D3HeatmapRenderer.js` - D3热力图核心渲染器
- [ ] 创建 `TriangleDataProcessor.js` - Triangle配置数据处理  
- [ ] 创建 `HeatmapCoordinator.js` - 坐标转换和自适应控制器

### 1.2 Triangle配置数据解析
- [ ] 实现 `parseTriangleLayout()` 函数解析JSON配置
- [ ] 提取光源位置数据 (optode_a, optode_b, optode_c)  
- [ ] 提取检测器位置数据 (optode_1, optode_2, optode_3, optode_4)
- [ ] 计算通道中点位置：`(光源位置 + 检测器位置) / 2`
- [ ] 验证数据：预期18光源 × 24检测器 = 432通道

### 1.3 基础Vue3组件框架
- [ ] 在 `BrainModeView.vue` 中添加SVG容器
- [ ] 设置SVG与brain_no_bg.png的叠加定位
- [ ] 创建响应式数据绑定：`watch(props.hboData)`
- [ ] 实现Vue3生命周期管理：`onMounted()`, `onUnmounted()`

## 🎨 阶段2：IDW插值算法实现

### 2.1 核心IDW算法
- [ ] 实现 `calculateDistance()` 函数计算2D欧氏距离
- [ ] 实现 `calculateIDWValue()` 函数执行反距离权重插值
- [ ] 设置影响半径参数：默认30mm，超出范围返回null
- [ ] 实现权重计算：`weight = 1 / distance^2` (power=2)
- [ ] 验证算法：使用静态测试数据验证插值结果

### 2.2 网格数据生成
- [ ] 创建 `generateHeatmapGrid()` 函数生成插值网格
- [ ] 设置网格密度：默认50×50，可配置
- [ ] 计算Triangle布局边界：188.72 × 110.29 mm
- [ ] 为每个网格点计算IDW插值值
- [ ] 过滤无效数据点：距离所有通道都超过影响半径的点设为透明

### 2.3 测试验证
- [ ] 创建模拟HbO数据用于测试
- [ ] 验证插值计算的数学正确性
- [ ] 验证边界控制：热力图应呈现自然三角形形状
- [ ] 性能测试：确保50×50网格的计算时间<100ms

## 🌈 阶段3：D3可视化渲染

### 3.1 等高线生成
- [ ] 使用 `d3.contours()` 基于网格数据生成等高线
- [ ] 设置等高线层级：10-15个层级确保平滑过渡
- [ ] 处理数据格式：将IDW结果转换为d3.contours需要的格式
- [ ] 生成SVG路径：将等高线转换为SVG path元素

### 3.2 颜色映射
- [ ] 实现 `d3.scaleSequential(d3.interpolateRdBu)` 双向颜色映射
- [ ] 红色表示正值(激活)，蓝色表示负值(抑制)
- [ ] 设置颜色域：根据HbO数据的实际范围动态调整
- [ ] 实现透明度控制：无数据区域完全透明

### 3.3 SVG渲染优化
- [ ] 设置SVG viewBox匹配Triangle坐标系
- [ ] 实现路径平滑：使用d3路径生成器确保等高线平滑
- [ ] 添加渐变效果：在等高线之间添加平滑过渡
- [ ] 优化渲染性能：减少DOM操作，批量更新

## 🔄 阶段4：自适应缩放系统

### 4.1 坐标系统转换
- [ ] Triangle物理坐标系 (mm) → SVG viewBox坐标系
- [ ] SVG坐标系 → HTML像素坐标系
- [ ] 参考 `heatmap_adaptive_demo.html` 的position/scale参数
- [ ] 实现坐标转换函数：`triangleToSVG()`, `svgToPixel()`

### 4.2 自适应定位机制
- [ ] 监听brain_no_bg.png图片尺寸变化
- [ ] 根据图片实际尺寸计算热力图位置和尺寸
- [ ] 实现绑定式缩放：热力图随图片同比例缩放
- [ ] 设置默认位置参数：position(0.5, 0.35), scale(0.65, 0.5)

### 4.3 响应式更新
- [ ] 监听窗口尺寸变化：`window.resize`
- [ ] 实现防抖更新：300ms延迟避免频繁重绘
- [ ] 缓存计算结果：避免重复的坐标转换计算
- [ ] 测试响应式效果：验证不同屏幕尺寸下的显示效果

## ⚡ 阶段5：数据集成与性能优化

### 5.1 实时数据集成
- [ ] 接入 `props.hboData` 实时数据流
- [ ] 处理数据格式：支持嵌套数组和Proxy对象
- [ ] 数据有效性验证：过滤NaN和无效数值
- [ ] 实现数据缓存：减少不必要的重新计算

### 5.2 性能优化
- [ ] 缓存Triangle通道位置：只在配置改变时重新计算
- [ ] 实现增量更新：只更新变化的数据点
- [ ] 优化DOM操作：使用`d3.transition()`实现平滑动画
- [ ] 内存管理：及时清理D3事件监听器和DOM引用

### 5.3 错误处理与降级
- [ ] Triangle配置加载失败的处理
- [ ] HbO数据异常的容错机制
- [ ] D3渲染错误的降级显示
- [ ] 添加开发模式下的详细日志输出

## 🧪 阶段6：测试验证与文档

### 6.1 功能测试
- [ ] 单元测试：IDW算法数学正确性
- [ ] 集成测试：完整数据流从HbO到可视化
- [ ] 视觉回归测试：与Python版本效果对比
- [ ] 性能基准测试：2Hz实时更新性能验证

## 🎭 Playwright MCP 自动化验证

> **重要**: 每个开发环节完成后，必须使用Playwright MCP进行自动化验证

### 阶段验证清单
- [ ] **阶段1完成**: Playwright验证SVG容器创建和基础布局
- [ ] **阶段2完成**: Playwright验证IDW算法输出和网格生成  
- [ ] **阶段3完成**: Playwright验证D3可视化渲染和颜色映射
- [ ] **阶段4完成**: Playwright验证自适应缩放和响应式布局
- [ ] **阶段5完成**: Playwright验证实时数据更新和性能表现
- [ ] **阶段6完成**: Playwright验证完整功能和回归测试

### MCP验证要点
1. **视觉验证**: 截图对比，确保热力图正确显示
2. **功能验证**: 模拟用户交互，验证响应正确性  
3. **数据验证**: 检查DOM结构，确保数据正确传递
4. **性能验证**: 监控渲染时间，确保满足实时要求
5. **兼容性验证**: 多分辨率测试，确保自适应效果

### 6.2 用户验证
- [ ] 静态数据展示效果验证
- [ ] 实时数据更新流畅度验证
- [ ] 不同屏幕尺寸适配验证
- [ ] 与其他训练模式的兼容性验证

### 6.3 技术文档
- [ ] API文档：核心模块接口说明
- [ ] 配置文档：参数调整和自定义选项
- [ ] 故障排除：常见问题和解决方案
- [ ] 性能调优：优化建议和最佳实践

## 🎯 技术参数配置

### 核心参数
- **网格密度**: 50×50 (可配置)
- **影响半径**: 30mm (可配置)
- **IDW幂次**: 2 (标准设置)
- **等高线层级**: 12层 (平滑过渡)
- **更新频率**: 2Hz (跟随数据流)
- **防抖延迟**: 300ms (性能优化)

### 坐标系统  
- **Triangle布局**: 188.72 × 110.29 mm
- **默认位置**: position(0.5, 0.35)
- **默认缩放**: scale(0.65, 0.5)
- **颜色映射**: RdBu_r (红蓝双向)

### 文件结构
```
src/components/training/modes/
├── BrainModeView.vue (主组件)
├── heatmap/
│   ├── D3HeatmapRenderer.js (D3渲染核心)
│   ├── TriangleDataProcessor.js (数据处理)
│   └── HeatmapCoordinator.js (坐标和缩放)
```

---

**开发状态**: 🚀 准备开始实施  
**预计完成时间**: 基础版本1-2天，完整版本3-4天  
**责任人**: Claude (AI Assistant)  

> 📝 **使用说明**: 每完成一个任务项，将 `[ ]` 改为 `[x]`，并在git提交中注明完成的具体功能。